rabbitmq:
  topicExchangeName: topic-exchange
  routingKeyName : routing.key
  directExchangeName: direct-exchange
  fanoutExchangeName: fanout-exchange
  username : guest
  password : guest
  host : localhost
  port : 5672

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/producer?createDatabaseIfNotExist=true
    username: root
    password: ueducation
    driver-class-name: com.mysql.cj.jdbc.Driver

